import { Container, Outer, Middle, Inner } from './loading.styles'

export function Loading() {
  return (
    <Container>
      <Outer>
        <Middle>
          <Inner></Inner>
        </Middle>
      </Outer>
    </Container>
  )
}
